{
  "builds": [
    { "src": "vite.config.*", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ],
  "framework": "vite",
  "outputDirectory": "dist"
}
